import { Section, InterviewsSlider, ResultsList } from '@/components';

const Home = () => {
  return (
    <Section intop width="1200px">
      <InterviewsSlider />
      <ResultsList />
    </Section>
  );
};

export default Home;
